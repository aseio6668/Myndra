// Hello World with context-aware logging
// This example demonstrates basic syntax and context-aware features

import std::io with capabilities("write")

fn main() {
    log("Starting Pomegranate program") if context == "dev"
    
    let message = "Hello, Pomegranate!"
    print(message)
    
    log("Program completed successfully") if context == "dev"
}
